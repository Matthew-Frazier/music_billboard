<h1><%= @artist.name %></h1>
<h4><%= @artist.genre%></h4>
<hr>
<%= link_to "View Songs", artist_songs_path(@artist) %>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= song.name %></li>
  <% end %>
  </ul>
<hr>
